const resultadoEResto = (dividendo, divisor) => {
  console.log(
    `A divisão do ${dividendo} por ${divisor} é: ${dividendo / divisor}`
  )
  console.log(`O resto dessa divisão é: ${dividendo % divisor}`)
}

resultadoEResto(4, 2)
