//função que retorna objeto

//factory simples

function criarPessoa() {
  return {
    nome: 'Ana',
    sobrenome: 'Silva'
  }
}

console.log(criarPessoa())

//criar um produto(obj) com nome, preco e desconto(que será fixo) que será recebido por parametros
